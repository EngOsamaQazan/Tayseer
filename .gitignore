# ═══════════════════════════════════════════════
#  Tayseer ERP (نظام تيسير) — .gitignore
# ═══════════════════════════════════════════════

# ─── Yii2 Console (generated by init) ─────────
/yii
/yii_test
/yii_test.bat

# ─── IDE / Editor ─────────────────────────────
.idea/
.vscode/
nbproject/
.buildpath
.project
.settings/
*.swp
*.swo

# ─── OS ───────────────────────────────────────
Thumbs.db
.DS_Store
desktop.ini

# ─── Composer ─────────────────────────────────
/vendor/
composer.phar
composer.lock

# ─── Node ─────────────────────────────────────
node_modules/

# ─── PHPUnit ──────────────────────────────────
phpunit.phar
/phpunit.xml

# ─── Runtime (generated by Yii2) ──────────────
/backend/runtime/
/frontend/runtime/
/console/runtime/
/api/runtime/

# ─── Generated web files (created by init) ────
/frontend/web/index.php
/frontend/web/index-test.php
/frontend/web/robots.txt
/backend/web/index.php
/backend/web/index-test.php
/backend/web/robots.txt

# ─── Uploaded content ─────────────────────────
/backend/web/images/*
!/backend/web/images/favicon.png
/backend/web/uploads/

# ─── Local config (generated by init) ─────────
/common/config/main-local.php
/common/config/params-local.php
/backend/config/main-local.php
/backend/config/params-local.php
/frontend/config/main-local.php
/frontend/config/params-local.php
/console/config/main-local.php
/console/config/params-local.php

# ─── Logs ─────────────────────────────────────
*.log

# ─── Docker volumes ───────────────────────────
mysql_data/

# ─── Database (dumps, migrations, seeds) ─────
/database/
*.sql

# ─── Scripts (contain server credentials) ──────
/scripts/temp/

# ─── Deploy config (contains credentials) ─────
deploy.config
sshpass.exe

# ─── Admin panel config (contains credentials) ─
/admin/config.php

# ─── Google Cloud credentials ─────────────────
backend/config/credentials/
*.gserviceaccount.json
tayseerai-*.json

# ─── Published assets (auto-generated) ────────
/backend/web/assets/*/

# ─── Binary tools ─────────────────────────────
/backend/bin/

# ─── Python cache ─────────────────────────────
__pycache__/
*.pyc

# ─── Inno Setup build output ─────────────────
scripts/deploy/Output/
scripts/deploy/platform-tools.zip
scripts/deploy/*.exe

# ─── Temp utility scripts ─────────────────────
check_cols.php
check_companies.php
test_api.php
fix-configs.py

# ─── Diagnostics / debug ──────────────────────
/backend/web/test-width.html
/docs/diagnostics/

# ─── Internal notes ───────────────────────────
/notes.md

# ─── Agent / Playwright session artifacts ─────
/.playwright-mcp/

# ─── UX/QA audit screenshots ──────────────────
# Screenshots used for one-off design reviews; the source of truth lives
# in design docs / tickets, not the repo.
/ux-audit/
/*.png
/*.jpeg
/*.jpg
!/social-preview.png

# ─── Throwaway diagnostic / migration scripts ─
# By convention every ad-hoc helper added during a debug session is
# prefixed with an underscore. They contain hard-coded local paths and
# DB credentials, so we keep them out of the repo.
/scripts/_*
/scripts/deploy/_*
/scripts/inspect_*
/scripts/migrate_*

# ─── Plain-text credentials (never commit) ────
/scripts/credentials.py
*cookies.txt
*.cookies.txt

# ─── Local-only operational scripts ───────────
# These contain hard-coded server passwords, GitHub OAuth tokens, and
# DB credentials inlined while we were doing migration / hot-fix work.
# Keep them out of the repo until they've been refactored to read
# secrets from credentials.py / env vars.
/scripts/*.py
/scripts/*.php
/scripts/*.sh
/scripts/*.bat
/scripts/check/
/scripts/debug/
/scripts/fix/
/scripts/verify/
/scripts/server/
/scripts/setup/
/scripts/upgrade/
/scripts/migration/
/scripts/dns/
/scripts/backup/
/scripts/deploy/
/scripts/misc/

# ─── Local agent / IDE state ──────────────────
/.claude/

# ─── Separate v2 monorepo (not tracked here) ──
/tayseer-v2/

# ─── One-off probe / dump artefacts at repo root ─
/probe_*.bat
/zajal_*.bat
/zajal_*.html
/z_api.html
/verify_backend.bat
